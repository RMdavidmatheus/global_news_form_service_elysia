{
  "name": "global_news_form_service",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "npm install -g bun && bunx prisma migrate deploy && bunx prisma generate && bun build --compile --minify-whitespace --minify-syntax --target bun --outfile server ./src/index.ts"
  },
  "dependencies": {
    "@elysiajs/cors": "^1.3.3",
    "@elysiajs/swagger": "^1.3.0",
    "@prisma/client": "^6.10.1",
    "@types/nodemailer": "^6.4.17",
    "elysia": "latest",
    "luxon": "^3.6.1",
    "nodemailer": "^7.0.3",
    "prisma": "^6.10.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@types/luxon": "^3.6.2",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
